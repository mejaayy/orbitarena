[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "sh -c 'npm run db:push || true' && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
